# 보안 안내

이 프로젝트는 시제품이며 보안 인증을 받은 제품이 아닙니다.

## 공개 저장소에 포함하지 않는 값

- 실제 장치의 Wi-Fi AP 비밀번호
- 실제 설치에 사용하는 32바이트 HMAC 키
- 개인 또는 현장별 식별정보

실제 값은 각 최종 스케치 폴더의 `project_config.h`에 두며 `.gitignore`로 제외합니다. 공개되는 `project_config.example.h`의 값은 컴파일 형식을 보여주기 위한 안전하지 않은 예시입니다.

## 알려진 제한

- 휴대폰 웹 UI는 AP 비밀번호 외 별도 계정·세션 인증이 없습니다.
- 유효한 설정값을 아는 사용자는 로컬 AP 범위에서 경보 시험 또는 해제를 요청할 수 있습니다.
- 64비트로 절단한 HMAC 태그는 패킷 변조 방지에 도움을 주지만 전체 시스템 보안 인증을 뜻하지 않습니다.
- 최초 유효 송신자를 RAM에서 잠그므로 외부 제어부 재부팅 뒤에는 다시 등록됩니다.

실제 설치 전 비밀번호와 키를 교체하고, 공개된 적이 있는 값은 재사용하지 마십시오.
