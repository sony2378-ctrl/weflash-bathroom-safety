# 펌웨어 구성

- `final/`: 천장 센서부 3.5.3 + 외부 제어부 3.5.0 확정 조합
- `diagnostics/`: 센서·입력·릴레이·ESP-NOW 개별 시험 코드

실제 설치에는 `final/`의 두 코드만 사용합니다. 두 장치는 패킷 v2, ESP-NOW
채널 6과 같은 HMAC 키를 사용해야 합니다.

ESP-NOW 진단 송신·수신 코드는 서로만 사용하는 구형 시험 패킷이므로 최종
펌웨어와 혼용하지 마십시오.
